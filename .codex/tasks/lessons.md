# Lessons Learned

- ユーザーが「今回のアップデート情報のみ」と指定した場合でも、追加指示で「既存履歴を削除しない」が来たら最新指示を優先し、履歴削除は行わない。
- アップデート情報の編集時は、`index.html` の履歴件数を変更前後で比較して意図しない大量削除がないことを検証する。
- 既存の共通強化を排他/ルート専用へ再分類する場合は、旧セーブの移行方針（リセット・返金・再配置）を先に定義し、移行後に未選択状態でも旧効果が残らないことを検証する。
